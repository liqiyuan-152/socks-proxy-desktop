## 1. 架构与依赖准备

- [x] 1.1 锁定首期 sing-box `v1.14.1` 的发行来源、GPLv3 许可证、发布提交 PGP 验证与资产 SHA-256 校验方式、Windows amd64 架构和打包位置；记录仅回环可访问、每次运行独立 Bearer secret 的 Clash API 配置和字段映射清单，并以真实固定版本验证活跃连接的目标、规则命中和出口链字段。明确不支持完成结果和失败字段；无法验证已承诺字段即阻塞本变更，不得开始后续实现任务。

  来源复核（2026-09-24）：`scripts/sing-box-release-manifest.md` 记录了上游 `v1.14.1` 标签对应的完整提交、GitHub Commit API 对该提交的有效 PGP 签名验证结果、发行 ZIP 与包内三个文件的独立 SHA-256、许可证和正式分发前的合规复核要求。本机暂存资源的三个 SHA-256 与清单一致；GitHub 签名验证不冒充本地独立的密钥信任验证。

- [x] 1.2 将 `src-tauri/src/` 拆分为领域模型、错误类型、配置存储、凭据存储、运行时编排、系统代理适配器、观测和 IPC 模块；验证 `cargo check --manifest-path src-tauri/Cargo.toml` 通过且模块边界不依赖前端实现。
- [x] 1.3 增加异步运行时、持久化、凭据存储和测试依赖，并锁定兼容的 Rust 1.78.0 版本；验证 `cargo build --manifest-path src-tauri/Cargo.toml` 与 `pnpm tauri:build:check` 通过。

## 2. 配置与凭据

- [x] 2.1 实现带 schema version 的代理档案、分流规则、活动选择、开机启动和运行时诊断保留设置的存储迁移；实现 7/30/90 天及默认 30 天保留、永久模式 100,000 条上限，验证单元测试覆盖重启恢复、稳定 ID、名称唯一性、端口和规则目标校验。
- [x] 2.2 实现操作系统凭据存储适配器和保留/替换/删除凭据 DTO；验证密码不会出现在配置文件、序列化 DTO、导出内容或测试日志中，编辑非凭据字段会保留密码，关闭认证会删除密码，导入配置要求重新提供凭据。
- [x] 2.3 提供档案、规则和设置的 Tauri 命令及统一错误码；实现首条匹配优先与未命中直连的规则编译，验证命令测试覆盖有效保存、无效输入、删除活动档案时的安全停机、规则排序和冲突、开机启动状态、导入导出和前端可显示的字段级错误。

## 3. 代理运行时与系统集成

- [x] 3.1 实现每 Windows 用户会话独占的 `RuntimeCoordinator`、运行时阶段和快照 DTO；将活动档案、规则和导入变更纳入候选修订事务，验证状态机测试覆盖第二实例拒绝、启动成功、启动失败、规则/全局热切换、直连、停止、配置提交失败回滚及运行时长的开始、保留和清除语义。
- [x] 3.2 实现 sing-box JSON 配置渲染、进程启动、监听端口健康检查、仅回环绑定的已验证活跃连接接口采集和进程清理；每次运行生成独立高熵密钥并限制临时配置访问。验证真实固定版本的集成测试覆盖活跃连接字段映射和外部地址不可访问，受控假内核测试覆盖进程身份、配置修订、异常退出和只停止本应用进程。
- [x] 3.3 实现 Windows 系统代理适配器、写入前持久化的原值/待写预期值/所有权令牌记录及用户触发的网络恢复命令；验证 Windows 集成测试覆盖应用设置、切换直连、启动失败、正常退出、下次启动恢复、手动恢复，以及逐字段写入中断后对原值、预期值与无法确认归属的中间值分别处理；中间值须保留现场及记录、阻止新接管，不覆盖第三方设置。
- [x] 3.4 将托盘模式菜单接入运行时命令和快照更新；验证菜单勾选仅在后端提交模式后更新，失败时保留最后已应用模式并可打开主窗口查看错误。

## 4. 观测与前端数据契约

- [x] 4.1 实现脱敏活跃连接快照和运行时诊断记录的读取、保留、详情复制与确认后清理；验证目标/规则/出口链字段、活跃连接数量、诊断保留上限、敏感字段剔除，以及活跃连接接口不可用时的观测降级。不得生成成功/失败事件、日聚合或历史连接。
- [x] 4.2 提供运行时快照、活跃连接快照、运行时诊断查询和 `runtime://snapshot` 事件；验证 IPC 测试覆盖启动、提交、回滚、异常退出及历史连接查询明确返回不可用。
- [x] 4.3 将状态、代理、规则、连接日志、设置、侧栏和底部状态栏替换为 Tauri IPC 数据源；在未启用 TUN 时显示系统代理覆盖范围，并将历史连接、成功率和失败详情显示为不可用。验证 Vitest 覆盖加载态、搜索与筛选、切换禁用、后端错误显示、无数据状态、活跃连接详情复制、运行时诊断清理、“查看更多”降级提示和设置确认操作，且移除示例成功状态。
- [x] 4.4 将未配置发布渠道的“检查更新”入口明确为不可用状态；验证前端不会发起网络请求或声称已完成更新检查。

## 5. 集成验证与交付

- [x] 5.1 增加跨模块回归测试，验证配置变更触发安全重载、切换失败回滚、退出恢复、凭据不泄露以及不从关闭事件或内核文本日志推断连接结果；验证 `cargo test --manifest-path src-tauri/Cargo.toml` 通过。
- [x] 5.2 在 Windows 已解锁桌面执行受控代理档案的端到端验收：验证规则、全局和直连模式、系统代理恢复、托盘交互及异常内核退出；记录实际结果，不将无法执行的系统级验收标记为通过。

  验收记录（2026-09-23，未完成）：Windows 已登录桌面中，通过界面保存并选择带认证的受控 SOCKS5 档案；全局代理成功启动 sing-box，经应用本地监听端口请求 Google `generate_204` 返回 HTTP 204。编辑分流规则时报告系统代理所有权不确定：旧运行版本的恢复记录预期 `AutoDetect=0`，而 Windows 实际未设置该字段；未通过猜测归属覆盖现场，规则、直连、托盘和异常退出的桌面验收均不得据此判定通过。修复后的独立 Windows 注册表回归测试验证未设置 `AutoDetect` 时接管及重启恢复成功，完整 Rust 测试 66/66 通过；真实桌面恢复与剩余流程仍待验收。

  后续验收记录（2026-09-24，未完成）：在用户明确同意临时设置 `AutoDetect=0` 后恢复原始设置，并逐字段核对恢复记录已清空；修复版真实桌面验证规则代理访问 Google 返回 HTTP 204，活跃连接分别报告 `google.com → selected-proxy` 与 `example.com → direct`；全局代理访问 Google 返回 HTTP 204；切换直连、手动恢复及内核异常退出均恢复系统代理。强制结束应用暴露旧版孤儿 sing-box 与残留私有配置，已按进程路径和配置路径核实后仅清理该测试进程及其私有目录。新增 Windows kill-on-close Job Object 后，真实桌面强退应用时受管内核立即退出；重启后系统代理恢复原值（`ProxyEnable=0`、原服务器 `127.0.0.1:7897`、`AutoDetect` 未设置），私有运行目录清空。随后收紧为仅清理带 Job Object 所有权标记的目录；最终构建再次验证强退后内核退出、重启恢复与目录清空，避免误删升级前仍在运行的旧版孤儿配置。托盘图标已定位，但两种自动右键方式均未观察到菜单，托盘模式及正常退出仍未完成真实验收。新增 Windows 单元测试可编译且严格 Clippy 通过，但测试可执行文件在进入测试前报 `STATUS_ENTRYPOINT_NOT_FOUND`，不得视为通过；本机 Rust 测试 63/63 通过。两条 smoke 分流规则、测试计划任务与脚本已清理；Windows `pnpm check`、`pnpm tauri:build:check`、OpenSpec 严格校验通过，最终 NSIS 安装包已生成且传回本机，经 SHA-256 核对一致。

  3.3 补充验证（2026-09-24）：Windows 测试程序缺少 Common Controls v6 清单，导致入口点加载失败；已为测试目标及正式应用统一嵌入与 Tauri 默认清单内容一致的清单，且避免重复资源。Windows 完整 `cargo test --locked` 运行 69/69 通过；新增隔离 HKCU 测试键的注册表集成测试，对写入中断后仍为原值、已达到预期值以及处于中间值三种情况分别验证恢复、保留证据与拒绝新接管。Windows `pnpm check` 与生产编译通过。托盘菜单已在真实桌面截图中确认可见；之前 UI Automation 枚举返回空集合不代表菜单未打开。5.2 仍需实际菜单模式切换及正常退出验证，未勾选。

  交付复核（2026-09-24，未完成 5.2）：再次核对已解锁桌面应用仍运行在直连且系统代理未被接管；交互式计划任务能在真实桌面打开“代理模式 / 状态 / 退出”菜单，但键盘与合成鼠标输入未可靠选择子菜单，后端快照始终为 `stopped`，因此不能声称托盘模式切换或正常退出通过。Windows 生产程序重新编译成功；第一次默认打包因下载 MSI 工具超时失败，限定 `--bundles nsis` 后成功生成安装包，并与本机副本核对 SHA-256 一致：`8494b25d8fd95ac0b8c269efc4ef115ca7fbc99948811835d027f5e9ecc5675a`。需人工在真实桌面操作托盘：直连 → 全局代理（核对后端快照和系统代理）→ 规则代理 → 直连 → 再次全局代理 → “退出”（核对进程结束和原系统代理恢复）；在此之前保持 5.2 未完成。

  后续交互复核（5.2 仍未完成）：再次确认受控应用进程存在、系统代理为原始直连状态。改用隐藏窗口的交互式任务后，托盘主菜单仍可显示，终端不再抢占前台；发送导航键后未出现子菜单，后端快照仍为 `stopped`。因此需要真实桌面中的人工选择来验证托盘回调，不能将自动化打开主菜单视为已通过模式切换或退出恢复。

  用户手动退出复核（2026-09-24，5.2 仍未完成）：用户报告已从系统托盘选择“退出”；远端核对 `socks-proxy.exe` 与 `sing-box.exe` 均不再运行，Windows 用户级代理仍为原始直连状态（`ProxyEnable=0`，原服务器 `127.0.0.1:7897`，`AutoDetect` 未设置）。诊断库在退出时记录“正在恢复系统代理”，代理所有权记录为零条。此次退出前的最近一次快照处于 `stopped` / 直连，所以只能确认直连状态下的托盘正常退出及无遗留代理设置，尚不能据此验证正在代理时通过托盘退出的恢复效果；托盘模式选择仍未实测。需重启应用并在托盘实际选择规则/全局/直连，最后在全局代理运行中点“退出”后复核进程、所有权记录和系统代理。

  托盘全局模式复核（2026-09-24，5.2 仍未完成）：将与最终 NSIS 构建相同的 release 程序及其随包内核复制到独立验收目录，在已登录 Windows 桌面启动；用户从托盘手动选择“全局代理”后，同一应用进程仍在运行，随包 sing-box 已启动，Windows 用户级代理从原始直连设置变为 `ProxyEnable=1`、`ProxyServer=127.0.0.1:50530`，内核临时配置的全局出口为 `selected-proxy`。初次请求 Google 失败，排查发现受控档案上游指向本机 `127.0.0.1:11080`，该临时测试转发端口当时未监听；恢复仅绑定回环的临时 SSH 转发后，经应用本地代理访问 Google `generate_204` 返回 HTTP 204。转发通道是本次测试设施，不应作为生产代理配置。仍须从托盘验证规则、直连以及全局运行时“退出”后的系统代理恢复，届时移除临时转发。

  托盘规则模式复核（2026-09-24，5.2 仍未完成）：用户在托盘切换后，同一应用进程保留、内核替换为新进程，Windows 系统代理改指向其新回环监听 `127.0.0.1:57402`；内核配置 `route.final=direct`、规则数为零。经应用端口访问 `example.com` 返回 HTTP 200，Google 直连超时；这仅验证规则模式下未命中直连的行为，尚未验证当前构建的规则命中走代理。需在界面临时新增一条 Google 域名走代理规则并验证实际流量，再清理测试规则。

  规则命中复核（2026-09-24，5.2 仍未完成）：用户在界面新增临时规则后，同一应用进程仍在运行，受管内核已重新生成配置并由系统代理指向 `127.0.0.1:62720`。核对实际配置包含 `domain=[www.google.com] → selected-proxy`、`route.final=direct`；经此监听端口请求 Google `generate_204` 返回 HTTP 204，经相同监听端口请求 `example.com` 返回 HTTP 200。已请求用户删除临时规则并从托盘切到全局直连；尚待核对恢复、再次全局代理运行时托盘退出及测试转发清理，因此不勾选 5.2。

  托盘代理运行中退出及临时规则清理（2026-09-24）：用户在规则代理运行中从托盘选择“退出”后，复核 `socks-proxy.exe` 和 `sing-box.exe` 均不再运行；Windows 用户级代理恢复至原始直连设置（`ProxyEnable=0`、原服务器 `127.0.0.1:7897`、`AutoDetect` 未设置），数据库代理所有权记录为零。退出后的三个运行目录仅剩空的 `job-owned` 标记文件，没有临时内核配置。持久数据库经完整性检查后，在 Windows 原位置留下两份修改前备份；离线以规则稳定 ID、匹配类型、目标和动作联合确认，仅删除 `www.google.com → proxy` 临时规则。回读远端数据库验证规则数为零、完整性正常，其他配置、诊断与所有权记录与备份一致。测试专用的回环 SSH 远端转发已取消，`127.0.0.1:11080` 无监听。尚需重启核对界面读取和托盘直连模式切换；完成前不勾选 5.2。

  重启后模式复核（2026-09-24，仍未完成）：在已解锁的桌面会话启动验收版后，应用于会话 1 持续运行。用户反馈已修改模式后，远端实际查到 `socks-proxy.exe` 和受管 `sing-box.exe` 均运行，系统代理为 `ProxyEnable=1`、`ProxyServer=127.0.0.1:52574`；内核配置 `route.final=selected-proxy`、规则数为零，因此实际处于全局代理，而不是待验证的全局直连。所选档案的上游仍指向已取消、无监听的临时回环转发 `127.0.0.1:11080`；不恢复测试转发或推断联网成功。待用户从托盘选择“全局直连”后再核对内核退出、原代理恢复与规则清理后的界面状态；5.2 仍不勾选。

  全局直连与验收完成复核（2026-09-24）：用户从托盘切换后，验收版 `socks-proxy.exe` 在原桌面会话继续运行，受管 `sing-box.exe` 已退出；Windows 用户级代理恢复为原始 `ProxyEnable=0`、`ProxyServer=127.0.0.1:7897`，`AutoDetect` 未设置。运行时诊断在 09:24:47 本地时间依次记录“正在恢复系统代理”和“代理运行时已停止”；持久数据库 `integrity_check=ok`、`proxy_ownership` 为零、重启后规则数为零，测试转发端口 `127.0.0.1:11080` 仍无监听。结合上述已解锁桌面的规则命中/未命中、全局代理通路、代理运行时托盘退出恢复和异常内核退出实测，5.2 的系统级验收完成。此轮未独立截图核对规则列表 UI；规则清理后的持久化值已核对。

- [x] 5.3 运行 `pnpm check`、`pnpm tauri:build:check`、`pnpm build` 和 `openspec validate implement-proxy-runtime-backend --strict`；验证产物不包含开发凭据、临时内核配置或未授权二进制。

  完成复核（2026-09-24）：使用符合项目约束的 Node 22.23.2、pnpm 10.33.0 重跑 `pnpm check`（Vitest 12/12、Rust Clippy 与格式检查通过；仅一条非阻断的 React Fast Refresh 提示）、`pnpm build` 和 `pnpm tauri:build:check`；`cargo test --locked` 为 63/63，`cargo +1.78.0 check --locked` 与 `cargo +1.78.0 build --locked` 均通过，OpenSpec 严格校验通过。Windows 包 SHA-256 与验收记录一致，Windows 内核资源的三个 SHA-256 与发行清单一致；`dist/` 和内核资源目录仅有已核对的静态资源及发行文件，未检出测试档案的临时凭据。Windows 平台特有的 69/69 Rust 测试及真实桌面行为结果见 5.2 的记录；未将 macOS 上未配置内核路径的条件测试计为真实内核验证。

  安装包内容复核（2026-09-24）：用 Windows 7-Zip 直接解开 SHA-256 为 `8494b25d8fd95ac0b8c269efc4ef115ca7fbc99948811835d027f5e9ecc5675a` 的原 NSIS 包，清单只有应用程序、三个锁定内核发行文件和六个 NSIS 自带插件／图片，无临时配置文件；内核三个文件的哈希正确。初次将旧 `bundle-inspection` 目录的 `6e8074e0…` 程序误认为原包内容；它早于原包，应忽略。保留原包后以现有 release 程序重新运行 `tauri bundle --bundles nsis`，新包 SHA-256 为 `e6273578153e21132d6a8aaea3bda29ec501b597b65002c115a700ff656a9e3f`，十个文件清单和内核哈希均再次核对。**直接解出的原包与新包应用程序完全相同**，SHA-256 均为 `9ef5abe4ebd7327c85f7994d64d61df793f77cd9f91991cae79eb2ccaa470ff7`；它与实际桌面验收版／未打包 release 程序的 `2b1eb8b04e998556bb8aeaf95b7db5945be6d1ae7038478cb53625a6c74657b9` 仅在 `.rdata` 节的 Tauri `__TAURI_BUNDLE_TYPE_VAR_UNK__` → `__TAURI_BUNDLE_TYPE_VAR_NSS__` 打包标记处相差 3 个字节，`.text`、其余节及程序长度完全一致。这是打包标记，不是另一版应用代码；5.3 可据实通过。原包在 Windows 验收目录保留了 `before-rebundle-8494.exe` 副本，本机同哈希副本仍可使用。
