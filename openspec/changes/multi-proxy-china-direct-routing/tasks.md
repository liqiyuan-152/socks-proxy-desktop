## 1. 兼容模型与迁移

- [x] 1.1 将持久化配置升为 v2，加入默认代理、规则出口引用和默认关闭的预设设置；测试覆盖旧配置有/无活动档案、顺序保留及待修复规则不静默直连。
- [x] 1.2 更新校验、配置服务和导入导出，拒绝悬空/停用出口、保留凭据隔离与事务回滚；运行 Rust 测试验证删除、停用、导入失败及 v1/v2 往返。

## 2. 多出口运行时

- [x] 2.1 将所有参与运行时的已启用代理凭据安全送入单个内核配置，生成稳定出站 tag 和按顺序映射的规则；使用单元测试和锁定内核 `sing-box check` 验证普通规则/全局模式及缺失凭据。
- [x] 2.2 调整模式前置条件、热切换和候选回滚，保证无默认代理的全局模式拒绝、未命中直连及出口不可用不降级；运行运行时和 Windows 系统代理回归测试。
- [x] 2.3 更新代理列表、规则编辑器和状态文案，支持默认代理及指定出口，并保留端口范围编辑；运行组件测试、`pnpm typecheck` 和 `pnpm lint`。

## 3. 国内直连预设

- [x] 3.1 确定中国域名与 IPv4/IPv6 规则集来源、许可证、版本、校验值和打包路径；以锁定 sing-box 版本对本地规则集执行 `check` 与离线加载测试，记录来源清单。
- [x] 3.2 生成用户规则优先、中国域名集及字面私有/中国 IPv4/IPv6 直连、最终默认代理的内核配置；以锁定内核的真实受管入口测试域名、字面 IP、IPv6、混合国内外 A/AAAA、重试与地址族回退，确认域名集外的域名始终走默认代理、不因解析候选 IP 误直连；验证解析失败、循环保护与规则集损坏时拒绝提交。
- [x] 3.3 在规则模式界面提供“国内直连”开关、仅域名集及字面 IP 直连的覆盖范围与数据版本说明，禁用无默认代理或无有效规则集的启用路径；运行组件测试和可访问性检查。

## 4. 路由解释与整体验证

- [x] 4.1 增加只读规则测试命令及界面，输入域名/IP 和端口返回命中阶段、规则及可确定的出口或规则集不可用等错误；域名集外无需解析即显示默认代理，但不推断最终拨号 IP 或连接成功。用相同配置对照真实内核连接集成测试，验证解释边界。
- [x] 4.2 运行 `pnpm check`、`pnpm build`、`pnpm rust:fmt`、`pnpm rust:clippy`、`cargo test --manifest-path src-tauri/Cargo.toml` 与可用的固定内核集成测试；检查 v1 升级、导出导入、离线启动、系统代理恢复及新文件行数约束。

验证记录：固定内核及 Rust 98 项、前端 55 项、Lint、类型检查、前端构建、Rust 格式与 Clippy、Tauri 无打包构建均通过。`pnpm check` 在全仓库格式检查处失败（32 个文件，含其他既有工作区改动）；后续检查已分别运行并通过。本变更触及的规则测试组件文件单独通过格式检查。混合 A/AAAA 和地址族重试由受控 HTTP 上游模拟；锁定内核入口证实域名原样交给同一默认代理，不验证真实公网 DNS 的最终拨号地址。
